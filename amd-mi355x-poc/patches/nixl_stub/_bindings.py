"""nixl._bindings stub.

Empty module; satisfies any `from nixl._bindings import ...` lookups by
allowing the import statement to succeed. Any actual symbol use will
raise AttributeError, which is desired for the agg-only PoC.
"""
